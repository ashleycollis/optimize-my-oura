# Copy to .env for local development

# Database (Postgres)
# Example: postgresql://USER:PASSWORD@HOST:PORT/DB
DATABASE_URL=postgresql://myoura_dev@localhost:5432/oura_db

# Django
# Provide a unique secret in your real .env
SECRET_KEY=dev-insecure-secret-change-me
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
CORS_ALLOWED_ORIGINS=http://localhost:5173
CSRF_TRUSTED_ORIGINS=http://localhost:5173

# Oura OAuth
OURA_CLIENT_ID=
OURA_CLIENT_SECRET=
# Must match the value configured in Oura developer portal
OURA_REDIRECT_URI=http://localhost:8000/api/auth/oura/callback/
# Comma-separated scopes; adjust as needed per Oura docs
OURA_SCOPES=email,personal,daily,heartrate,workout,tag,session,enhanced_readiness
